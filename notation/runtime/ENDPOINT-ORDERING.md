# Local endpoint-ordering correction

Runtime 0.5.0-draft.2. `ddn-layout.js` computes geometry-ordered compatible slots; `ddn-placement.js` runs bounded paired slot/side refinement on checked paths. See [the specification](../spec/26-local-endpoint-ordering.md) and [patch evidence](../patches/endpoint-ordering/VERIFICATION.md).
