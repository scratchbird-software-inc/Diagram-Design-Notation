# Design decisions for approval

This is an author-proposed baseline. Independent user research and professional signoff are pending.

| ID | Decision | Reason | Alternative and cost |
|---|---|---|---|
| ADR-01 | Separate Designer route/application; keep Studio unchanged. | Visual construction and source editing serve different workflows. | Turning Studio into a large modal editor risks regressing source authoring. |
| ADR-02 | Use the DDN-native SVG and scene as the first interaction host. | Preserves all projection geometries and patched routing. | diagram-js is an optional evaluated interaction adapter; React Flow requires custom projection/port handling. |
| ADR-03 | One canonical source workspace and one history. | Cross-view identity and Git review remain reliable. | A parallel UI shape model requires complex and lossy synchronization. |
| ADR-04 | Three inspector scopes: Meaning, This view, Details. | Users see where changes apply; advanced properties remain discoverable. | One universal property list overwhelms and mixes semantics with appearance. |
| ADR-05 | Eight starter families plus profile-filtered exact-kind search. | Exposes tasks, not hundreds of equal-weight icons. | One generic shape silently erases semantic distinctions; hundreds of buttons impede discovery. |
| ADR-06 | Explicit create-drop pins; click Add leaves position free. | Distinguishes user position intent from automatic layout. | Automatically pinning every generated node defeats layout; ignoring drops frustrates users. |
| ADR-07 | Draft validation is separately typed, not global error suppression. | Users can build a process incrementally without weakening publication. | Strict-only construction blocks Start-before-End; permissive-only hides unsafe operations. |
| ADR-08 | Add existing, duplicate definition, copy representation and replica stay distinct. | A visual duplicate must not invent storage or authority. | Generic copy/paste without intent conflates model and view. |
| ADR-09 | Graph gestures are disabled on data-bound geometry. | Changing chart/date geometry can change facts. | Free dragging makes an attractive but false diagram. |
| ADR-10 | Production operations use prepared, revision-checked change plans. | Undo, imports, members and references remain atomic. | Calling several committing helpers creates partial failure and confusing history. |

Approval should record exact artifact hashes and accepted residual scope. Use `signoff-template.json`; do not prefill reviewer identities or approvals.

## Implementation log

| ADR | Implementation | Notes |
|---|---|---|
| ADR-05 | Implemented in prototype by ED-001 (2026-09-20) | Eight groups come from kind-ui-map.json palette_group; all 188 kinds searchable. |
| ADR-09 | Matrix cells edited via structured sheet, never drag, by ED-002 (2026-09-20) | RACI/CRUD/relations cells commit one `setMatrixAssignments` batch transaction; cell contributors select the assignment in the companion graph view. |
| ADR-09 | Chart geometry stays data-bound; bar-drag offers record edit, by ED-003 (2026-09-20) | Chart marks reject drag with a "Values set geometry — edit the record" toast; record values, marks and bindings commit through the command layer. |
| ADR-09 | Timeline bars move dates via previewed data edits, never geometry, by ED-004 (2026-09-20) | Bar drag converts pointer movement to whole-day date changes with a live interval preview and commits through `setTimelineDates`; contradiction (DDN-PJ042) and cycle (DDN-PJ043) reject before commit. |
| ADR-08 | Attach-existing-cause never clones the definition, by ED-005 (2026-09-20) | Attaching an existing cause to a second fishbone branch creates only the cause-verb relation: one semantic identity with distinct runtime occurrence paths (VE-AC-057); reuse, duplicate and replica stay distinct. |
| ADR-10 | Panel moves/binds are single prepared transactions, by ED-006 (2026-09-20) | Every panels gesture (rename, respan, add/remove, item move, child-view bind) validates the candidate array and re-plans in a scratch workspace, then commits one revision-checked write; DDN-PJ020/021/PJ009, DDN-QP001/002/003 and canvas DDN-PJ080/081/083 reject before commit. |
| ADR-09 | Decision rows edited via typed sheet; overlap/budget shown, never hidden, by ED-007 (2026-09-20) | Rule CRUD/reorder, typed predicate controls, hit-policy editing, analysis and fixture evaluation run through the command layer; an overlapping unique-hit rule commits as a draft with the DDN-QD004 witness displayed and export blocked (VE-007), and a budget-exceeded analysis is reported unestablished, never success. |
| ADR-10 | Reconnection is one prepared, revision-checked transaction with preview, by ED-008 (2026-09-20) | Drag and inspector paths share `prepareReconnect` validation (member XOR port, member_endpoints, allow_self, source/target kind lists) and the five-part impact preview; one span edit commits against the expected revision with the relation id, label, properties and other endpoint byte-identical; route overrides stay retained. |
| ADR-07 | Draft issues are separately typed (incomplete vs error), never globally suppressed, by ED-010 (2026-09-20) | `Commands.validate`/`classifyCode` type profile-completeness codes (`DDN-PF*`, `DDN-PJ016`, `DDN-QD0*`, `DDN-QL*`) as `incomplete` under policy `design` and `error` under `review`; every other code is always `error`, renderer diagnostics pass through, and no code is ever dropped. |
| ADR-08 | Add-existing focuses or restores, never clones; alias requests get an explicit restriction response, by ED-009 (2026-09-20) | `Commands.occurrences` addresses every appearance as `occ:<viewId>:<definitionId>`; addExistingToView focuses/restores or appends a select ref, same-view duplicates answer already-present · one-appearance-per-view (VE-AC-036), and relation-occurrence/unsupported-descriptor requests answer explicit unsupported responses with zero source change. |
| ADR-03 | Lanes are view frames in canonical source; no parallel UI grouping model, by ED-011 (2026-09-20) | `Commands.createLane`/`renameLane`/`resizeLane`/`assignToLane`/`unassignFromLane` stage view-`frame` edits (create/rename/X-Y-W-H/fit/member assignment) through scratch re-renders; every drag- or picker-driven assignment opens a preview naming the exact member changes and stating that no containment, ownership or placement relationship is created (covers VE-AC-040); one-lane-per-element policy; `x_partition` is maintained only for `uml.activity@1` with DDN-PJ114 as the commit-time authority. |
| ADR-09 | Sequence order is declaration order edited as data; pixels are never time, by ED-012 (2026-09-20) | `Commands.moveDeclaration`/`reorderLifelines`/`addSequenceMessage`/`setMessageReturn`/`setMessageLabel`/`removeSequenceMessage` edit the `uml.sequence@1` view as shared source: reorders move declaration spans only (endpoints and `x_return` byte-identical, VE-AC-062), returns are `x_return` true-or-absent, two-click lifeline connect and the sheet list are equivalent (VE-006), silent participants surface DDN-PJW03 (VE-007), and no placement control exists while LIVE021 rejects layout overrides (VE-AC-063). |
| ADR-05 | Templates are task-level starters over the mapped kinds, not new kinds, by ED-013 (2026-09-20) | `Commands.CANVAS_TEMPLATES`/`createCanvasFromTemplate` generate a fixed-grid panels view plus one synthetic starter `note` per block in one staged transaction — the grids are copied from the landed RT-013/014/016 profile tables (SWOT uses `panels.basic@1`), the scratch render is the pre-commit authority for DDN-PJ020/021/PJ009 and the canvas DDN-PJ080/081/083 guards, and nothing beyond the starter notes is invented (spec ch.06). |
