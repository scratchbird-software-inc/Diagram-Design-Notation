{
  "name": "data-design-notation",
  "version": "0.6.0-beta.1",
  "private": true,
  "description": "Data Design Notation (DDN): a model-first diagram language, reference runtime, visual designer proposal, and open standard draft.",
  "license": "GPL-2.0-or-later",
  "workspaces": [
    "notation",
    "designer"
  ],
  "scripts": {
    "test": "npm --prefix notation test && npm run test:packaging && npm --prefix designer test",
    "test:packaging": "node tools/tests/packaging.js",
    "test:core": "npm --prefix notation run test:core",
    "build:sdk": "npm --prefix notation run build:sdk",
    "serve": "node tools/serve.js"
  },
  "engines": {
    "node": ">=22"
  }
}
